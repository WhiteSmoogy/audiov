[package]
name = "audiov"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
whisper-rs = "0.11"
evdev = "0.12"
nix = { version = "0.29", features = ["signal"] }
hound = "3"

[dev-dependencies]
tempfile = "3"
