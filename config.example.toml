# audiov configuration example

[whisper_cpp]
# Local whisper model path
model = "models/ggml-large-v1.bin"

# Runtime tuning
threads = 4
temperature = 0.0

# Optional acceleration flags (depends on your whisper.cpp build)
use_gpu = false


[whisper]
# Transcription backend: cpp | remote
backend = "cpp"

[whisper_remote]
# Enable remote Whisper-compatible HTTP API.
enabled = false

# Compatible with OpenAI audio transcription API format.
endpoint = "https://api.openai.com/v1/audio/transcriptions"
model = "whisper-1"

# Put your API key here.
api_key = ""

# HTTP timeout in seconds.
timeout_secs = 60

[language_detection]
# Enable language identification (LID) before transcription.
enabled = false

# One-shot detection per push-to-talk recording session.
mode = "per_session"

# Allowed language whitelist for low-latency routing.
allowed_languages = ["zh", "en"]

# If true, detected/fallback language is passed to whisper inference.
# Keep this false if you want to skip language inference entirely.
use_detected_language_for_inference = false

# If confidence is below threshold, runtime falls back to default_language.
confidence_threshold = 0.65

# Default language used when detection fails / low confidence / not whitelisted.
default_language = "zh"

[hotkey]
# KDE / Plasma global shortcut backend via org.kde.KGlobalAccel.
enabled = true

# Toggle recording on each press.
shortcut = "Meta+H"

# Stable IDs shown in KDE global shortcuts settings.
component_unique = "audiov"
component_friendly = "audiov"
action_unique = "toggle-recording"
action_friendly = "Toggle Recording"

[paste]
# Output mode: command | clipboard | fcitx5
mode = "command"

# Command used to trigger paste event for focused app when mode = "command".
# This default sequence means Ctrl+Shift+V in ydotool scan codes.
command = ["ydotool", "key", "29:1", "42:1", "47:1", "47:0", "42:0", "29:0"]

# Fcitx5 addon DBus endpoint used when mode = "fcitx5".
fcitx5_service = "org.fcitx.Fcitx5"
fcitx5_path = "/org/freedesktop/Fcitx5/Audiov"
fcitx5_interface = "org.fcitx.Fcitx5.Audiov1"

[recorder]
# Native recorder backend: auto | pipewire | pulseaudio | alsa
backend = "auto"

# Optional input device/source name for the selected backend.
# - PipeWire: pw node/target name used with `pw-cat --target`
# - PulseAudio: source name used with `parec --device`
# - ALSA: hw/plughw PCM name used with `arecord -D`
# input_device = ""

[targets]
# Accuracy objective for zh/en in local benchmark conditions.
zh_en_accuracy_target = 0.95

[performance]
# Product preference: lower end-to-end latency.
priority = "latency"
